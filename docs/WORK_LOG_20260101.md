# 작업 로그 - 2026년 1월 1일

## 📋 작업 요약

가상환경 설정 및 테스트 인프라 수정 작업을 완료했습니다.

---

## 🔧 수행한 작업

### 1. 가상환경 설정 문제 해결

**문제:**
- Python 3.13 free-threaded 버전이 `cffi` 패키지와 비호환
- 필요한 패키지들이 설치되지 않음

**해결:**
- Python 3.11.9로 가상환경 재생성
- requirements.txt의 모든 패키지 설치 완료 (43개)

**설치된 주요 패키지:**
```
pytest==8.3.4
selenium==4.27.0
allure-pytest==2.13.5
requests==2.32.3
pandas==2.2.3
python-dotenv==1.0.1
```

**명령어:**
```bash
rm -rf .venv
py -3.11 -m venv .venv
.venv/Scripts/pip install -r requirements.txt
```

---

### 2. 테스트 Fixture 추가

**문제:**
- API 테스트 실행 시 fixture 없음 오류 발생
- UI 테스트에서 일부 fixture 누락

**해결:**
`src/tests/conftest.py`에 다음 fixture 추가:

#### API Test Fixtures
```python
@pytest.fixture(scope="function")
def api_env():
    """API 환경 설정 객체"""
    from utils.api_utils import APIEnv
    return APIEnv()

@pytest.fixture
def send_get_request(api_env):
    """GET 요청 보내기"""
    return api_env.send_get_request

@pytest.fixture
def attach_response(api_env):
    """API 응답 첨부"""
    return api_env.attach_response

@pytest.fixture
def allure_attach_response(api_env):
    """API 응답 전체 첨부 (상태 코드 포함)"""
    return api_env.allure_attach_response
```

#### UI Test Fixtures
```python
@pytest.fixture
def kurly_search_page(driver):
    """마켓컬리 검색 페이지 객체"""
    from src.pages.kurly_search_page import KurlySearchPage
    return KurlySearchPage(driver)

@pytest.fixture
def test_credentials_invalid():
    """잘못된 로그인 자격증명"""
    return {
        "username": "invalid_user@example.com",
        "password": "wrong_password123"
    }
```

---

### 3. Page Object 메서드 수정

#### 3.1. test_cart_management.py 오류 수정

**문제:**
```python
# Line 73 - 잘못된 호출
kurly_cart_page.go_to_cart()  # ❌ KurlyCartPage에 없는 메서드
```

**해결:**
```python
# Line 73 - 올바른 호출
kurly_main_page.go_to_cart()  # ✅ KurlyMainPage에 있는 메서드
```

**파일:** `src/tests/ui/test_cart_management.py:73`

---

#### 3.2. KurlySearchPage 팝업 메서드 추가

**문제:**
- `test_add_product_to_cart.py`에서 호출하는 팝업 관련 메서드 누락
- 4개 메서드 없음: `increase_quantity_of_nth_product_in_alt()`, `decrease_quantity_of_nth_product_in_alt()`, `click_add_to_cart_in_alt()`, `is_add_to_cart_success()`

**해결:**
`src/pages/kurly_search_page.py`에 팝업 관련 메서드 추가:

```python
# Locators - 팝업 내 요소
QUANTITY_UP_BUTTON_ALT = (By.XPATH, "//button[@aria-label='수량올리기']")
QUANTITY_DOWN_BUTTON_ALT = (By.XPATH, "//button[@aria-label='수량내리기']")
ADD_TO_CART_BUTTON_ALT = (By.XPATH, "//button[@class='css-ahkst0 e4nu7ef3']")
SUCCESS_MESSAGE_ALT = (By.XPATH, "//*[contains(text(),'장바구니에 상품을 담았습니다.')]")

def increase_quantity_of_nth_product_in_alt(self, times: int = 1) -> None:
    """팝업에서 수량 증가"""
    for _ in range(times):
        self.click(self.QUANTITY_UP_BUTTON_ALT)
        self.sleep(0.3)

def decrease_quantity_of_nth_product_in_alt(self, times: int = 1) -> None:
    """팝업에서 수량 감소"""
    for _ in range(times):
        self.click(self.QUANTITY_DOWN_BUTTON_ALT)
        self.sleep(0.3)

def click_add_to_cart_in_alt(self) -> None:
    """팝업에서 장바구니 담기 버튼 클릭"""
    self.click(self.ADD_TO_CART_BUTTON_ALT)

def is_add_to_cart_success(self) -> bool:
    """장바구니 담기 성공 메시지 확인"""
    return self.is_displayed(self.SUCCESS_MESSAGE_ALT, timeout=5)
```

---

#### 3.3. test_ui_search.py 타이포 수정

**문제:**
```python
# Line 101 - 타이포
def test_search_non_existent_product(self, drer, keyword):  # ❌ drer
```

**해결:**
```python
# Line 101 - 수정
def test_search_non_existent_product(self, driver, keyword):  # ✅ driver
```

**파일:** `src/tests/ui/test_ui_search.py:101`

---

## 📝 Git 커밋 기록

### Commit 1: Fixture 추가 및 메서드 호출 수정
```
50ba2eda86f Fix: Add missing test fixtures and correct method call

- Add API test fixtures (api_env, send_get_request, attach_response, allure_attach_response)
- Add kurly_search_page fixture
- Add test_credentials_invalid fixture for login tests
- Fix test_cart_management.py: change kurly_cart_page.go_to_cart() to kurly_main_page.go_to_cart()
```

**변경된 파일:**
- `src/tests/conftest.py` (+44, -50)
- `src/tests/ui/test_cart_management.py` (+1, -1)

---

### Commit 2: Page Object 메서드 추가 및 타이포 수정
```
c2165f3e955 Add missing popup methods to KurlySearchPage and fix typo

- Add popup-related methods to KurlySearchPage:
  * increase_quantity_of_nth_product_in_alt()
  * decrease_quantity_of_nth_product_in_alt()
  * click_add_to_cart_in_alt()
  * is_add_to_cart_success()
- Fix typo in test_ui_search.py: drer → driver
```

**변경된 파일:**
- `src/pages/kurly_search_page.py` (+47, -2)
- `src/tests/ui/test_ui_search.py` (+1, -1)

---

## ✅ 테스트 결과

### 성공한 테스트
- ✅ `test_add_product_to_cart.py::test_add_product_to_cart_flow` - PASSED
- ✅ `test_blank_search.py::test_search_invalid_product` - PASSED
- ✅ `test_ui_login.py::test_login_with_empty_credentials` - PASSED
- ✅ `test_ui_login.py::test_login_page_elements_displayed` - PASSED

### 실패한 테스트
- ❌ `test_ui_login.py::test_login_with_invalid_credentials` - 로그인 에러 메시지 locator 찾기 실패
- ❌ `test_cart_management.py::test_add_multiple_items_to_cart` - 장바구니 아이콘 클릭 실패
- ❌ `test_ui_search.py` 일부 테스트 - 검색 결과 요소 찾기 실패

**실패 원인:**
- Selenium locator가 웹사이트 구조 변경으로 인해 요소를 찾지 못함
- 마켓컬리 웹사이트의 HTML 구조가 변경되었을 가능성

---

## 🔍 알려진 이슈

### 1. API 테스트 미실행
- `.env` 파일에 TMDB API 키가 설정되지 않음
- API 테스트 실행 시 환경변수 오류 발생
- 해결 방법: `.env` 파일 생성 후 `TMDB_API_KEY` 설정 필요

### 2. UI 테스트 Locator 업데이트 필요
- 일부 웹 요소의 XPath/CSS Selector가 변경됨
- 마켓컬리 웹사이트 구조 확인 후 locator 업데이트 필요

---

## 📦 환경 정보

**Python 버전:** 3.11.9
**가상환경 위치:** `.venv/`
**테스트 프레임워크:** pytest 8.3.4
**브라우저 자동화:** Selenium 4.27.0
**리포팅:** Allure 2.13.5

---

## 🎯 향후 작업 제안

1. **Locator 업데이트**
   - 마켓컬리 웹사이트 현재 구조 분석
   - 실패하는 테스트의 locator 수정

2. **API 테스트 환경 설정**
   - `.env` 파일 생성
   - TMDB API 키 발급 및 설정

3. **추가 테스트 케이스 검증**
   - 나머지 UI 테스트 실행 및 확인
   - 에러 수정 및 테스트 안정화

---

**작업 완료 일시:** 2026년 1월 1일
**작업자:** Claude Code (Claude Sonnet 4.5)
